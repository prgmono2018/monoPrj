"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getSearchableTypes = void 0;

const isDocumentType = type => type.type && type.type.name === 'document';

const isSanityType = type => type.name.startsWith('sanity.');

const getSearchableTypes = schema => schema.getTypeNames().map(typeName => schema.get(typeName)).filter(type => isDocumentType(type) && !isSanityType(type));

exports.getSearchableTypes = getSearchableTypes;